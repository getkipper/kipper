---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: usageplans.kipper.run
spec:
  group: kipper.run
  names:
    kind: UsagePlan
    listKind: UsagePlanList
    plural: usageplans
    singular: usageplan
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.rate
      name: Rate
      type: integer
    - jsonPath: .spec.burst
      name: Burst
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: UsagePlan is the Schema for the usageplans API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              UsagePlanSpec defines rate and quota limits that API keys can reference.
              A plan lives in the same environment namespace as the apps and keys it
              governs.
            properties:
              burst:
                description: |-
                  Burst is the token-bucket size: how many requests may arrive at
                  once before the rate applies.
                minimum: 1
                type: integer
              displayName:
                description: DisplayName is the human-readable plan name shown in
                  the console.
                maxLength: 128
                type: string
              quota:
                description: |-
                  Quota caps total requests per calendar period. Unset means no
                  period quota, only the rate limit.
                properties:
                  period:
                    description: Period is the calendar window the quota applies to.
                    enum:
                    - day
                    - week
                    - month
                    type: string
                  requests:
                    description: Requests is the number of requests allowed per period.
                    format: int64
                    minimum: 1
                    type: integer
                required:
                - period
                - requests
                type: object
              rate:
                description: |-
                  Rate is the steady-state request rate in requests per second,
                  enforced per authz replica (best effort, AWS-style).
                minimum: 1
                type: integer
            required:
            - burst
            - rate
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
